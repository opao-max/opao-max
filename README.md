### 刘嘉欣 · AI Engineer

Building Agent systems that understand web pages, execute code safely, and automate real work.

---

**ChromeAI** — AI browser assistant. Accessibility Tree → Ref Bridge → annotated screenshots → 13 computer actions with coordinate/ref dual-path execution. Agent safety layer with permission management, navigation isolation, and context auto-compaction.

**LocalBox** — E2B-compatible local sandbox gateway. Pluggable multi-backend (Docker Engine API / OrbStack JSON-RPC over UDS / Apple Container XPC via CGo). 70+ endpoints, state reconciliation, zero CLI forking.

**lark-wiki-migrate** — Feishu wiki migration CLI. Block API BFS transfer with batch failure auto-degradation. Cross-org resource import, comment migration with 3-tier fallback, mindnote Playwright automation.

**wiki-cross-tenant** — Feishu cross-tenant incremental migration. Copy Node API, bitable async replication race handling, link rewriting across docx/sheet/bitable. Replaced a ¥1M+ vendor solution.

**agent-readiness** — Vendor-free browser automation skill for AI coding agents (Claude Code, Codex, Cursor).

**e2b-go-sdk** — Go client SDK for E2B sandbox lifecycle, command execution, and filesystem operations.

**firecracker-sdk-rs** — Rust SDK for Firecracker microVM management.

---

`Go` `TypeScript` `Python` `Rust` `React` `Chrome Extension` `MCP` `RAG` `LLM Fine-tuning` `Docker`

[Email](mailto:2709255936@qq.com) · [GitHub](https://github.com/opao-max)
